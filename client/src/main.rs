fn main() {
    // Minimal placeholder. The Ratatui terminal UI and the IPC bridge to the
    // Python runtime (see runtime/src/orion/transport) land in a later task.
    println!("ORION client — Ratatui terminal UI coming soon.");
}
